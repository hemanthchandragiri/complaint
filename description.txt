Tables Sql
1.complaint Table
	1.username--varchar(100)
	2.rollno--varchar(15)
	3.time--char(20)
	4.email--varchar(100)
	5.day--char(20)
	6.complaint--text
	7.date--date
	8.complaintdate--date
	9.week--char(20)
	10.image--varchar(100)
2.login
	1.username--varchar(100)
	2.password--varchar(100)
		the database name is testproject 
if you want to use this codes create a folder in your htdocs folder and in that folder
create another folder called images to get the images uploaded
				